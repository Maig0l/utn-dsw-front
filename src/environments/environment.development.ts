// TODO: I f'd up the env directory name. Pls move src/environment/*.ts to here and !!update references!!.

import * as devEnv from '../enviroment/enviroment';

export const environment = devEnv.environment;
